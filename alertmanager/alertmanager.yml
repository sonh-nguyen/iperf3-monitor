route:
  receiver: default
  group_by: ["alertname", "site"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  # No real alert channel wired up yet (Telegram/Slack/email). Example:
  #
  # - name: default
  #   telegram_configs:
  #     - bot_token: "<token>"
  #       chat_id: <chat_id>
  #       api_url: https://api.telegram.org
  - name: default
